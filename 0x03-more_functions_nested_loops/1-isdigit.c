#include "holberton.h"

/**
 * _isdigit - checks for digits from 0 - 9
 *
 *@c: integer
 * Return: 1 if true, 0 if false
 */
int _isdigit(int c)
{
	if (c > 47 && < 58)
	{
		return (1);
	}
	return (0);
}
